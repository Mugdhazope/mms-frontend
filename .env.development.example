# Local `npm run dev`. Copy to `.env.development` or `.env.local` if you need overrides.

# Where Vite proxies `/api` (dev only; not used in `npm run build`)
# VITE_DEV_API_ORIGIN=http://127.0.0.1:8000

# VITE_RECAPTCHA_SITE_KEY=
# VITE_RECAPTCHA_MANUAL_SITE_KEY=

# VITE_USE_SEED=true
